raise ImportError(
    "app.slices.aerobic_pipeline is DEPRECATED (9-state pipeline). "
    "The archived source lives in _deprecated/aerobic_pipeline.py. "
    "Active orchestrator: app.slices.cardiorespiratory.orchestrator (CardioOrchestrator)."
)
